These binaries are own built versions of gmp-6.3.0 and mpfr-4.2.1. They
have been compiled under Raspberry OS 13 (64 bit) by using the --with-pic
option: first gmp have to be compiled and then mpfr (by using it).

The --with-pic option is mandatory.
